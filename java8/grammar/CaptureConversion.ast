/**
 * A capture variable, the fresh type variable a wildcard type argument is
 * replaced by during capture conversion (JLS SE8 5.1.10).
 *
 * Unlike a declared type variable a capture variable can have a lower bound,
 * which it gets from a lower bounded (super) wildcard.
 */
CaptureVariable : TypeVariable ::= LowerBound:Access*;
