aspect Java9NameCheck{
    refine Java8NameCheck eq VariableDeclarator.nameProblems() {
        Collection<Problem> problems = refined();
        if(name().equals("_")){
            problems.add(error("Underscore is not a valid identifier")); // TODO check javac error message
        }
        return problems;
    }
}