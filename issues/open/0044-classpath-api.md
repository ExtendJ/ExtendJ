# Classpath API

Implement an API to modify the classpath. Currently the classpath is parsed from the "-classpath" argument. This parsing is completely opaque and difficult to modify. JastAddJ should construct the classpath in a more dynamic manner which allows for extensions to alter the classpath.
